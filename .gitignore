# Dependencies
node_modules/
.pnp
.pnp.js
.pnpm-store/

# Build outputs
dist/
.build/
dsh-appserver/bin/
app/target/
/target/
app/flowix-desktop/icons/android/
app/flowix-desktop/icons/ios/
app/flowix-desktop/target/
app/flowix-desktop/binaries/

# Tauri-generated native scaffolds
app/flowix-desktop/gen/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dev launcher runtime artifacts
*.pid
*.stderr.path

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Env
.env
.env.local
.env.*.local

# Tauri / Rust
**/*.rs.bk
Cargo.lock.bak
# Rust 静态库产物 ── 仅 cargo build 生成，不应入库（曾经误提交过 libflowix_anthropic_check.rlib）
*.rlib

# Test / cache
coverage/
.nyc_output/
*.tsbuildinfo

# Local artifacts
test.json
app/flowix-desktop/tauri*.production.local.json

# Cloudflare / Wrangler local dev state (miniflare cache, R2 sqlite/blobs)
.wrangler/

# Apple Developer ID signing (scripts live in scripts/apple-signing/,
# but the actual keys/certs must never be committed)
*.p12
*.csr
*.cer
flowix_devid.key
.app-store-connect/
developerID_application.*
# iOS TestFlight pipeline additions (Apple Distribution cert,
# App Store Connect API key, App Store provisioning profile)
*.p8
*.mobileprovision
appstoreconnect/
